{
  "name": "@lingviora/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "check": "npm run typecheck && npm test",
    "db:generate": "drizzle-kit generate",
    "db:local": "wrangler d1 migrations apply DB --local --config wrangler.jsonc",
    "db:production": "wrangler d1 migrations apply DB --remote --env production --config wrangler.jsonc",
    "config:production": "node scripts/render-production-config.mjs",
    "secrets:production": "node scripts/render-production-secrets.mjs",
    "deploy:production": "wrangler deploy --env production --config wrangler.jsonc"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260831.1",
    "@types/node": "^24.3.0",
    "drizzle-kit": "0.31.10",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "wrangler": "4.128.0"
  }
}
